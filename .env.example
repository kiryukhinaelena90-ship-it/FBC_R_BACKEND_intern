# Optional shared secret between Vercel and R
FBC_R_API_KEY=

# Keep false until P1/P2 dynamic financial adapter + MC runtime gate is closed
FBC_ENABLE_P1_RUNNER=false

# Optional override after the validated production MC CSV is deployed
# FBC_MC_FILE=/app/data/processed/fbc_monte_carlo_draws.csv
